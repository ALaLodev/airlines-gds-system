package com.gds.airline.auth_service.entity;

public enum Role {
    ROLE_CUSTOMER, // Cliente final
    ROLE_AGENCY, // Agencia de viajes que conecta al GDS
    ROLE_ADMIN // Administrador del sistema de la aerolínea
}
