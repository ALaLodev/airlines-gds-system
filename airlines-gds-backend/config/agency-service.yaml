server:
  port: 8086

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    hostname: localhost

jwt:
  secret: "TuClaveSecretaSuperLargaParaFirmarLosTokensGDS2026"

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agency_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
