server:
  port: 8082

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
jwt:
  secret: "TuClaveSecretaSuperLargaParaFirmarLosTokensGDS2026"

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flights_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true